# Test Credentials

## Owner / Sovereign
- Email: `kyndsmiles@gmail.com`
- Password: `Sovereign2026!`
- Role: owner / founder
- Token is returned at `POST /api/auth/login` — use `token` or `access_token`
  field from the JSON response, store in `localStorage` as `zen_token`.

No other pre-seeded accounts. Guest-mode token is the literal string `guest_token`.
