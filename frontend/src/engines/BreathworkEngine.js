/**
 * BreathworkEngine.js — Render-Mode Adapter (V68.79 pillar batch)
 * Pulls the Breathwork page into the matrix slot without nav.
 */
import React from 'react';
import Breathing from '../pages/Breathing';
export default function BreathworkEngine() { return <Breathing />; }
