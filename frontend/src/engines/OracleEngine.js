/**
 * OracleEngine.js — Render-Mode Adapter
 *
 * Pulls Oracle & Tarot directly into the matrix slot. No router push.
 */
import React from 'react';
import Oracle from '../pages/Oracle';

export default function OracleEngine() {
  return <Oracle />;
}
